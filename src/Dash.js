import React from 'react';

const styles = {
    height: '100vh',
    width: '100vw',
    backgroundColor: 'black',
    border: '5px solid white'
};

function Dash(){
    return (
        <main style={styles}></main>
    );
}

export default Dash;